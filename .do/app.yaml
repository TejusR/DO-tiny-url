name: whale-app
region: nyc

alerts:
  - rule: DEPLOYMENT_FAILED

databases:
  - name: tiny-url-db
    engine: PG
    version: "17"
    production: false

envs:
  - key: DATABASE_URL
    scope: RUN_AND_BUILD_TIME
    type: GENERAL
    value: ${tiny-url-db.DATABASE_URL}
  - key: ENVIRONMENT
    scope: RUN_TIME
    type: GENERAL
    value: production
  - key: PUBLIC_BASE_URL
    scope: RUN_TIME
    type: GENERAL
    value: ${APP_URL}

ingress:
  rules:
    - component:
        name: api
      match:
        path:
          prefix: /

services:
  - name: api
    environment_slug: python
    github:
      repo: TejusR/DO-tiny-url
      branch: main
      deploy_on_push: false
    source_dir: /
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 8080
    run_command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8080
    health_check:
      http_path: /ready
    liveness_health_check:
      http_path: /health

jobs:
  - name: migrate
    kind: PRE_DEPLOY
    environment_slug: python
    github:
      repo: TejusR/DO-tiny-url
      branch: main
      deploy_on_push: false
    source_dir: /
    run_command: python -m alembic upgrade head
